Atue como um redator técnico especialista em documentação para projetos de sistemas embarcados. Sua tarefa é criar um arquivo README.md completo e bem formatado para um projeto de Raspberry Pi Pico W.

**Nome do Projeto:**
PicoTermoControl: Sistema de Controle de Temperatura PI com Atuadores Coordenados e Interface Web

**Instruções Gerais:**
- O formato de saída deve ser Markdown.
- Utilize emojis para destacar os títulos das seções.
- Utilize tabelas para listas de hardware e pinagem.
- Utilize blocos de código para comandos de terminal e estruturas de arquivos.
- A linguagem do documento deve ser Português do Brasil.
- Baseie toda a documentação nos arquivos de código que fornecerei abaixo do prompt.

---

**Estrutura do README.md (Siga esta ordem):**

**1. Título e Badges:**
   - Comece com o nome completo do projeto como título principal (`#`).
   - Adicione 4 badges (escudos) para: Linguagem (C/C++), Hardware (Raspberry Pi Pico W), Algoritmo (Controle PI) e Interface (Web UI).

**2. Descrição Breve (`### 📝 Descrição Breve`):**
   - Analise o `main.c` para entender o propósito geral.
   - Escreva um parágrafo conciso explicando o que o projeto faz. Destaque os componentes principais (Pico W, AHT20), o algoritmo central (Controle PI) e as formas de interação (Web, OLED, Serial). Mencione a atuação coordenada do servo e da ventoinha.

**3. Funcionalidades Principais (`### ✨ Funcionalidades Principais`):**
   - Crie uma lista de funcionalidades com marcadores.
   - Comece cada item com o emoji "✅".
   - Extraia as funcionalidades dos códigos: Controle PI, atuadores coordenados, dashboard web, interface local com menus, sistema de status com LED/Buzzer.

**4. Hardware Necessário (`### ⚙ Hardware Necessário`):**
   - Crie uma tabela com as colunas "Componente", "Quant." e "Observações".
   - Analise a seção de `#define` no `main.c` e as bibliotecas incluídas (`aht20.h`, `ssd1306.h`, etc.) para listar todos os componentes de hardware necessários.

**5. Conexões e Configuração (`### 🔌 Conexões e Configuração`):**
   - Crie seções para cada barramento de comunicação (I2C0, I2C1) e para os demais pinos (Atuadores, Feedback e Controles).
   - Extraia a pinagem diretamente dos `#define` no `main.c`.
   - Adicione uma nota de aviso sobre a alimentação externa para o servo e o driver do motor.

**6. Começando (`### 🚀 Começando`):**
   - Crie duas subseções: "Pré-requisitos de Software" e "Configuração e Compilação".
   - Em "Pré-requisitos", liste as ferramentas padrão (Pico SDK, CMake, etc.).
   - Em "Configuração e Compilação", explique que o usuário deve editar as credenciais de Wi-Fi no `main.c` e forneça um bloco de código com os comandos `git clone`, `mkdir build`, `cd build`, `cmake ..`, `make`, e `cp`. Use placeholders como `[URL_DO_SEU_REPOSITORIO]`.

**7. Estrutura do Projeto (`### 📁 Estrutura do Projeto`):**
   - Analise o `CMakeLists.txt` e a lista de arquivos para criar uma representação em árvore da estrutura de diretórios do projeto.

**8. Solução de Problemas (`### 🐛 Solução de Problemas`):**
   - Crie uma lista com marcadores para problemas comuns.
   - Sugira soluções baseadas no hardware utilizado: problemas de conexão Wi-Fi (verificar credenciais), falha no sensor (verificar I2C), atuadores não funcionando (verificar alimentação externa), dashboard não carregando (verificar IP e rede).

---

**Arquivos de Código para Análise:**

**CMakeLists.txt:**
```cmake
[COLE O CONTEÚDO DO SEU CMakeLists.txt AQUI]